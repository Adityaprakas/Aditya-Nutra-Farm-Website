{
  "entities": {
    "UserProfile": {
      "title": "UserProfile",
      "description": "User profile with language preference stored in Firestore",
      "type": "object",
      "properties": {
        "uid": {
          "type": "string",
          "description": "The unique user ID from Firebase Auth"
        },
        "preferredLanguage": {
          "type": "string",
          "enum": ["en", "hi"],
          "description": "User's preferred language"
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp when preferred language was updated"
        }
      },
      "required": ["uid", "preferredLanguage", "updatedAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": "UserProfile",
      "description": "Collection of user language and profile configs"
    }
  }
}
